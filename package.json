{
  "name": "l457-website",
  "version": "1.0.0",
  "description": "L457 Music Website",
  "main": "index.html",
  "scripts": {
    "start": "serve",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod",
    "deploy:preview": "netlify deploy",
    "validate": "node scripts/validate-deployment.js",
    "setup:firebase": "node scripts/setup-firebase.js"
  },
  "dependencies": {
    "@dataconnect/generated": "file:src/dataconnect-generated",
    "firebase": "^10.7.1",
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
